{
  "name": "helpdesk-ia",
  "version": "0.1.0",
  "private": true,
  "description": "HelpDesk IA - monorepo full stack para mesa de ayuda con asistencia de IA.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm install --prefix client && npm install --prefix server && npm run build --prefix client && npm run prisma:generate",
    "prisma:generate": "prisma generate --schema=server/prisma/schema.prisma",
    "start": "npm run start --prefix server",
    "test": "npm run test --prefix server",
    "db:migrate": "npm run db:migrate --prefix server",
    "db:seed": "npm run db:seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "prisma": "6.11.1"
  }
}
